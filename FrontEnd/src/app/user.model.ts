export interface UserResponse {
  id: number;
  username: string;
  email: string;
  balance: number;
  token: string;
}

export interface UserData{
  id: number;
  username: string;
  email: string;
  balance: number;
}